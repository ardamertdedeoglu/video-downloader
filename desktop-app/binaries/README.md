# Binaries Directory

This folder stores the yt-dlp and ffmpeg binaries.
They are downloaded automatically on first run.

## Platform-specific folders (for bundling)
- `win32/` - Windows binaries
- `darwin/` - macOS binaries  
- `linux/` - Linux binaries
